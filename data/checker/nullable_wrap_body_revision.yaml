openapi: 3.1.0
info: {title: t, version: "1.0.0"}
paths:
  /test:
    post:
      operationId: testOp
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: "null"
                - $ref: "#/components/schemas/SomeObject"
      responses:
        "200": {description: OK}
components:
  schemas:
    SomeObject:
      type: object
      properties:
        a: {type: string}
        b: {type: integer}
      required: [a]
