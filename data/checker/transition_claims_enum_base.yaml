openapi: 3.1.0
info: {title: t, version: "1.0.0"}
paths:
  /test:
    post:
      operationId: testOp
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                mode:
                  type: string
                  enum: [alpha, beta]
      responses:
        "200": {description: OK}
