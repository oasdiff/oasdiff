openapi: 3.1.0
info: {title: t, version: "1.0.0"}
paths:
  /t:
    get:
      parameters:
        - name: q
          in: query
          schema:
            type: [string, "null"]
        - name: filter
          in: query
          schema:
            type: object
            properties:
              tag:
                type: [string, "null"]
      responses:
        "200": {description: ok}
