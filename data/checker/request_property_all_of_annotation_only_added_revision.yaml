openapi: 3.0.0
info:
  title: ACME
  version: 1.0.0

paths:
  /pets:
    post:
      operationId: updatePets
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
              allOf:
                - title: "annotation-only subschema"
                  description: "no validation effect — accepts every instance the parent does"
      responses:
        "200":
          description: Updated
