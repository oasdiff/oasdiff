openapi: 3.0.0
info:
  title: Test
  version: "1.0"
paths:
  /example:
    get:
      operationId: getExample
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    required: ["foo"]
                    properties:
                      foo: { type: string }
                      bar: { type: string }
                  - type: object
                    required: ["bar"]
                    properties:
                      foo: { type: string }
                      bar: { type: string }
