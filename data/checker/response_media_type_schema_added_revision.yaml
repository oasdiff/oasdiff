openapi: 3.0.0
info:
  title: revision
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: string
