openapi: 3.1.0
info:
  title: Test
  version: "1.0"
paths:
  /items:
    post:
      operationId: createItem
      requestBody:
        content:
          application/json:
            schema:
              type: array
              prefixItems:
                - type: string
                - type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  tags:
                    type: array
                    prefixItems:
                      - type: string
