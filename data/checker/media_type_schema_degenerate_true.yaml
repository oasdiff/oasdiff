openapi: 3.1.0
info:
  title: Test
  version: "1.0"
paths:
  /items:
    post:
      operationId: createItem
      requestBody:
        content:
          application/json: {schema: true}
      responses:
        "200":
          description: OK
          content:
            application/json: {schema: true}
