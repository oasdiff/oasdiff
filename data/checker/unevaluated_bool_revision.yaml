openapi: 3.1.0
info:
  title: Test
  version: "1.0"
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              unevaluatedProperties: false
              allOf:
                - $ref: '#/components/schemas/Base'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                unevaluatedProperties: false
                allOf:
                  - $ref: '#/components/schemas/Base'
components:
  schemas:
    Base:
      type: object
      properties:
        name:
          type: string
