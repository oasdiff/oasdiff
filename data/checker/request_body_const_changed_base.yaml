openapi: 3.1.0
info:
  title: Sample API
  version: 1.0.0
paths:
  /products:
    post:
      summary: Create a new product
      operationId: createProduct
      requestBody:
        content:
          text/plain:
            schema:
              type: string
              const: "FixedValue"
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                status:
                  type: string
                  const: "active"
        description: RequestBody
