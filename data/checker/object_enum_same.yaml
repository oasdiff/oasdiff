openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test:
    get:
      operationId: getTest
      parameters:
        - name: zone
          in: query
          schema:
            type: string
            enum:
              - basement
              - x:
                  reuven
              - second-floor
      responses:
        '200':
          description: OK
