openapi: 3.1.0
info:
  title: request param type test
  version: 1.0.0
paths:
  /api/v1.0/groups:
    post:
      operationId: createOneGroup
      parameters:
        - in: query
          name: token
          schema:
            type:
              - array
              - "null"
            items:
              type:
                - string
                - "null"
      responses:
        "200":
          description: ok
