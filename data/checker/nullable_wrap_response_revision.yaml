openapi: 3.1.0
info: {title: t, version: "1.0.0"}
paths:
  /test:
    get:
      operationId: testOp
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    oneOf:
                      - type: "null"
                      - type: string
                        enum: [ok, degraded]
