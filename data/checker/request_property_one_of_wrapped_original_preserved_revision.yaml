openapi: 3.0.0
info:
  title: Test
  version: "1.0"
paths:
  /example:
    post:
      operationId: postExample
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: ["foo", "bar"]
                  properties:
                    foo: { type: string }
                    bar: { type: string }
                    baz: { type: string }
                - type: object
                  required: ["ref"]
                  properties:
                    ref: { type: string }
      responses:
        "200": { description: Success }
