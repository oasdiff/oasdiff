openapi: 3.0.0
info: {title: t, version: "1.0.0"}
paths:
  /x:
    get:
      operationId: getX
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/A"
                  - $ref: "#/components/schemas/B"
components:
  schemas:
    A:
      type: object
      required: [id]
      properties: {id: {type: string}}
    B:
      type: object
      required: [id]
      properties: {id: {type: string}}
