openapi: 3.0.0
info:
  title: revision
  version: 1.0.0
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                id:
                  type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: string
