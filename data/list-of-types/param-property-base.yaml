openapi: 3.0.0
info:
  title: Parameter Property Test API
  version: 1.0.0
paths:
  /test:
    get:
      parameters:
        - name: filter
          in: query
          schema:
            type: object
            properties:
              mode:
                oneOf:
                  - type: string
                  - type: integer  
                  - type: boolean
      responses:
        '200':
          description: Success