openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /api:
    get:
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    anyOf:
                      - type: string
                      - type: number
                      - type: boolean
                  metadata:
                    oneOf:
                      - type: string