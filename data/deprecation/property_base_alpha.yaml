openapi: 3.0.0
info:
  title: Property Deprecation Test
  version: 1.0.0
paths:
  /test:
    post:
      summary: Test endpoint
      operationId: createTest
      x-stability-level: alpha
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TestRequest'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestResponse'
components:
  schemas:
    TestRequest:
      type: object
      properties:
        name:
          type: string
          description: Name property
        oldField:
          type: string
          description: Old field that will be deprecated
    TestResponse:
      type: object
      properties:
        id:
          type: string
          description: ID property
        legacyField:
          type: string
          description: Legacy field that will be deprecated
