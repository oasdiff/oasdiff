openapi: 3.0.0
info:
  title: Response Property Deprecation Test
  version: 1.0.0
paths:
  /test:
    get:
      summary: Test endpoint
      operationId: getTest
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TestResponse'
components:
  schemas:
    TestResponse:
      type: object
      properties:
        id:
          type: string
          description: ID property
        legacyField:
          type: string
          description: Legacy field that will be deprecated
