openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test:
    get:
      parameters:
        - name: PageNumber
          in: query
          schema:
            type: integer
        - name: PageSize
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: OK