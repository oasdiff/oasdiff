openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    get:
      parameters:
        - name: PageNumber
          in: query
          schema:
            type: integer
        - name: PageSize
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: OK