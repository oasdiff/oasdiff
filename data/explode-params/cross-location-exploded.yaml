openapi: 3.0.0
info:
  title: Test API
  version: 1.0.0
paths:
  /test:
    get:
      parameters:
        - name: userParams
          in: query
          style: form
          explode: true
          schema:
            type: object
            properties:
              userId:  # Same name as cookie param but in different location
                type: string
      responses:
        '200':
          description: OK