openapi: 3.0.3
info:
  title: upgrade-test
  version: '1.0'
paths:
  /items:
    get:
      parameters:
        - in: query
          name: q
          schema:
            type: string
            nullable: true
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: integer
                exclusiveMinimum: true
                minimum: 0
                example: 7
